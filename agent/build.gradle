dependencies {
	implementation project(":base")
	
	implementation "org.ow2.asm:asm-commons:${ASM}"
}

jar {
	manifest {
		attributes([
			"Premain-Class": "net.luis.agent.PreMain",
			"Can-Redefine-Classes": "true",
			"Can-Retransform-Classes": "true"
		])
	}
}
